server:
  port: 8080

# 로컬에서도 BasicCard JSON을 확인할 수 있게 localhost 이미지로 지정.
# (카카오가 접근하진 못하지만 카드 직렬화 결과를 로컬에서 검증 가능)
kakao:
  image-base-url: http://localhost:8080/images

logging:
  file:
    name: /home/ubuntu/logs/server.log

spring:
  profiles:
    active: local
  application:
    name: 로컬-숫자야구

  # 로컬 MySQL (docker-compose.yml로 기동). 운영 RDS와 같은 8.0 + utf8mb4로 통일.
  datasource:
    url: jdbc:mysql://localhost:3306/baseball?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useUnicode=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: baseball
    password: baseball

  # 개발 중 스키마 자동 생성/갱신
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

# Swagger UI: http://localhost:8080/swagger-ui.html
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method   # 메서드(GET/POST 등) 순 정렬
